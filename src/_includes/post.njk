---
layout: base.njk
---

{% if date or tags %}
<div class="kv">
    {% if date %}
    <div>
        <div>Date</div>
        <p>{{date}}</p>
    </div>
    {% endif %}
    {% if tags %}
    <div>
        <div>Tags</div>
        <ul>
        {% for tag in tags %}
            <li>
                <a href="/tag/{{tag}}">{{tag}}</a>
            </li>
        {% endfor %}
        </ul>
    </div>
    {% endif %}
</div>
<hr/>
{% endif %}

{% if tags %}
    {% set islooking = true %}
    {% for tag in tags %}
        {% if islooking and tagList[tag].showOnPost %}
            {% set islooking = false %}
            <div class="card" {% if tagList[tag].color %} style="background-color: {{tagList[tag].color}}" {% endif %}>
                <p>{{tagList[tag].icon if tagList[tag].icon}} <a href="/tag/{{tag}}">{{tag}}</a></p>
                <p class="pad">{{tagList[tag].description | safe if tagList[tag].description}}</p>
            </div>
        {% endif %}
    {% endfor %}
{% endif %}

{{content | safe}}

{% if not backlinks | length == 0 %}
    <hr/>
    <h2>What links here?</h2>
    <ul>
        {% for backlink in backlinks %}
            <li>
                <a href="{{backlink.url}}">{{backlink.title}}</a>
            </li>
        {% endfor %}
    </ul>
{% endif %}